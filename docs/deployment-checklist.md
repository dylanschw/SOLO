# SOLO Deployment Checklist

SOLO is a mobile first Progressive Web App built with React, TypeScript, Vite, Supabase, and React Query.

## Pre-deployment checks

Run these locally before every deployment:

```powershell
npm run build
npm run test